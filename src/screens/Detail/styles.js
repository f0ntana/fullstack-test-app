import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    infos: {
        padding: 10
    },
    author: {
        fontSize: 12,
        fontWeight: 'bold'
    },
    title: {
        fontWeight: '800',
        fontSize: 20,
        color: '#f4a354',
        paddingBottom: 10,
    },
    description: {
        fontSize: 12
    }
});

export default styles;
